<%= link_to route,
            class: "w-full px-3 py-2 side-menu-fade-in hover:bg-surface
                    border-b-2 border-b-transparent hover:border-primary
                    #{'border-r-4 border-primary text-primary font-bold border-b-0 hover:border-b-transparent' if condition}",
            style: "transition-delay: #{delay};" do %>
  <span class="inline-block w-full font-display"><%= content %></span>
<% end %>